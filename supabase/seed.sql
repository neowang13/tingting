-- Run after creating the first Supabase Auth administrator.
-- Replace :admin_user_id with the Auth user's UUID.

-- insert into public.admin_profiles (user_id, display_name)
-- values (':admin_user_id', 'Ting Ting Xu');

-- Fixed section rows are seeded by the application provisioning command after
-- validating the JSON content against the code-owned Zod registry.
