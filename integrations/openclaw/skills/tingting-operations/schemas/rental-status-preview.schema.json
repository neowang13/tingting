{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["action", "expectedVersion"],
  "properties": {
    "action": { "enum": ["publish", "unpublish", "archive"] },
    "expectedVersion": { "type": "string", "format": "date-time" }
  }
}
